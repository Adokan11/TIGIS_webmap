# TIGIS_webmap

for the webmap run app.py then open http://127.0.0.1:5000

you will need `flask`, `geopandas`, and `pathlib` and all their dependencies

create a .env file like this:

    ORACLE_USER=s1234567
    ORACLE_PASSWORD=***